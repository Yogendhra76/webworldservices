# Copy this file to .env and fill in real values, OR set these
# as "Environment Variables" in your hosting provider's dashboard.

# The password used to log into /admin.html
ADMIN_PASSWORD=change-this-password

# The port the server listens on (most hosts set this for you automatically)
PORT=3000
